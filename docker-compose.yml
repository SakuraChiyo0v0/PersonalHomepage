version: '3.8'

services:
  app:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - ./backend/data:/app/backend/data
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=4200
    restart: unless-stopped